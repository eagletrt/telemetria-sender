import { Options } from "../types";
export declare class Logger {
    private log;
    constructor(options: Options);
    private formatPath;
    info(str: string, tag?: string, path?: boolean): void;
    succ(str: string, tag?: string, path?: boolean): void;
    warn(str: string, tag?: string, path?: boolean): void;
    error(str: string, tag?: string, path?: boolean): void;
}
